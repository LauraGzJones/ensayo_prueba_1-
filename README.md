# ensayo_prueba_1-
# entrada
dato = int(input("Ingrese dato: "))

# proceso
resultado = dato * 2  # ejemplo

# salida
print(f"Resultado: {resultado}")
#operadores 
#suma = a + b
resta = a - b
multiplicacion = a * b
division = a / b
resto = a % b
potencia = a ** b
“descuento” → resta porcentaje
“ganancia/interés” → multiplica
“promedio” → suma y divide
“porcentaje” → divide por 100
